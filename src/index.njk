{% extends '_templates/base.njk' %}

{% block page_title %}ICJ Final Project 2024{% endblock %}

{% block page_description %}The index search description for this page goes here.{% endblock %}

{% block content %}
<div class="container">
  <h1 class="center", font-family="'Times New Roman', Times, serif", font-size="200px">ALE DE LA FUENTE</h1>
  <p>Ale de la Fuente is a journalism and media student at The University of Texas at Austin. Currently, she is a photographer in practice and works as a Senior Photographer for The Daily Texan.</p>
  <h2 class="center">Music Photography</h2>
  <p>Click below to see more photos from each artist.</p>
  <div class="row">
    {% for capital in capitals.capitals %}
      <div class="col">
                <img src="img/{{capital.image1}}" alt="{{capital.alt1}}" width="300"/>
                <h3 class="center"><a href="capitals/{{ capital.slug }}.html">{{ capital.Name }}</a></h3>
      </div>
    {% endfor %}
  </div>
  <h2>Bookstores loop</h2>
  <p>This loops through data that came from a Google Sheet example.</p>
  <hr>
  {% for store in bookstores.stores %}
    <h3><a href="{{ store.url }}" target="_blank">{{ store.name }}</a></h3>
    <p>{{ store.address}}, {{ store.city }}, {{ store.state }} {{ store.zip }}</p>
    <hr>
  {% endfor %}
</div>
{% endblock %}